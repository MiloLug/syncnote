import { createStore } from 'vuex';

import note from './note';
import user from './user';

export default createStore({
    state: {
      
    },
    mutations: {
    },
    actions: {
    },
    modules: {
        note,
        user
    }
});
